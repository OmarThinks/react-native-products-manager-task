# Testing

## Preparations:

- Update expo
- remove dev build, move to Expo Go
- revise the code
- Change theme colors
- RTKQuery: should go to API calls
  - Create API Endpoints
  - make the api endpoints return the same responses
  - Link RTKQuery with the endpoints
  - add some delay in the endpoints

## Actual Testing:

- Install Jest (If not installed)
- Create a simple function in a utils folder, call the file simpleMath.tsx, sum()
- Create a test for the sum() function
- In package.json, add a test command
- Run the tests
- Get into snapshots, component after component
- run the tests
- Find out how to test a screen
- Test a simple dummy screen (settings screen for example)
- Create a mock api with different data
- Test the home screen with the request from the mock api
- run the tests
